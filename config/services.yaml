<<<<<<< HEAD
services:
  _defaults:
    autowire: true
    autoconfigure: true
    public: false

  App\:
    resource: '../src/'
    exclude:
      - '../src/DependencyInjection/'
      - '../src/Entity/'
      - '../src/Kernel.php'
      - '../src/Tests/'
      - '../src/Controller/'

  App\Service\:
    resource: '../src/Service'
    autowire: true
    public: true

  App\Repository\:
    resource: '../src/Repository'
    autowire: true

  App\Controller\:
    resource: '../src/Controller'
    autowire: true
    autoconfigure: true
    public: true

  App\Security\AppAuthenticator:
    arguments:
      $entityManager: '@doctrine.orm.entity_manager'
      $router: '@router'
=======
# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    onboarding.cloudinary.cloud_name: '%env(default::CLOUDINARY_CLOUD_NAME)%'
    onboarding.cloudinary.unsigned_preset: '%env(default::CLOUDINARY_UNSIGNED_PRESET)%'
    onboarding.public_base_url: '%env(default::ONBOARDING_PUBLIC_BASE_URL)%'
    onboarding.libretranslate.api_url: '%env(default::LIBRETRANSLATE_API_URL)%'
    onboarding.libretranslate.api_key: '%env(default::LIBRETRANSLATE_API_KEY)%'
    app.mailer_from: '%env(string:MAILER_FROM)%'
    app.groq_api_key: '%env(string:GROQ_API_KEY)%'
    app.groq_resume_model: '%env(string:GROQ_RESUME_MODEL)%'
    app.groq_review_model: '%env(string:GROQ_REVIEW_MODEL)%'
    app.groq_user_summary_model: '%env(string:GROQ_USER_SUMMARY_MODEL)%'
    app.google_client_id: '%env(default::GOOGLE_CLIENT_ID)%'
    app.google_client_secret: '%env(default::GOOGLE_CLIENT_SECRET)%'
    app.google_redirect_uri: '%env(string:default::GOOGLE_REDIRECT_URI)%'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
    App\Onboarding\AttachmentUploadConfiguration:
        arguments:
            $cloudName: '%onboarding.cloudinary.cloud_name%'
            $unsignedPreset: '%onboarding.cloudinary.unsigned_preset%'

    App\Onboarding\PublicUrlConfiguration:
        arguments:
            $publicBaseUrl: '%onboarding.public_base_url%'

    App\Onboarding\LibreTranslateService:
        arguments:
            $apiUrl: '%onboarding.libretranslate.api_url%'
            $apiKey: '%onboarding.libretranslate.api_key%'

    App\Onboarding\LocalTaskAttachmentStorage:
        arguments:
            $projectDir: '%kernel.project_dir%'

    App\Service\ApplicationNotificationMailer:
        arguments:
            $fromAddress: '%app.mailer_from%'
            $projectDir: '%kernel.project_dir%'

    App\Service\ResumeAutofillService:
        arguments:
            $groqApiKey: '%app.groq_api_key%'
            $model: '%app.groq_resume_model%'

    App\Service\ApplicationReviewAssistantService:
        arguments:
            $groqApiKey: '%app.groq_api_key%'
            $model: '%app.groq_review_model%'
            $projectDir: '%kernel.project_dir%'

    App\Service\UserRoleSummaryService:
        arguments:
            $groqApiKey: '%app.groq_api_key%'
            $model: '%app.groq_user_summary_model%'

    App\Service\GoogleOAuthService:
        arguments:
            $clientId: '%app.google_client_id%'
            $clientSecret: '%app.google_client_secret%'
            $redirectUri: '%app.google_redirect_uri%'

    Smalot\PdfParser\Parser: ~
>>>>>>> OnboardingCoordination
