security:
<<<<<<< HEAD
    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            lazy: true
            provider: app_user_provider
            form_login:
                login_path: app_login
                check_path: app_login
            logout:
                path: app_logout
    access_control:
        - { path: ^/login$, roles: PUBLIC_ACCESS }
security:
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'
        App\Entity\User:
            algorithm: auto
=======
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

>>>>>>> OnboardingCoordination
    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email
<<<<<<< HEAD
=======

>>>>>>> OnboardingCoordination
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            lazy: true
            provider: app_user_provider
<<<<<<< HEAD
            custom_authenticator: App\Security\AppAuthenticator
            logout:
                path: app_logout
    access_control:
        - { path: ^/login$, roles: PUBLIC_ACCESS }
        - { path: ^/recruiter-dashboard, roles: ROLE_RECRUITER }
security:
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'
        App\Entity\User:
            algorithm: auto
    
    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email
    
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            lazy: true
            provider: app_user_provider
            custom_authenticator: App\Security\AppAuthenticator
            logout:
                path: app_logout
            remember_me:
                secret: '%kernel.secret%'
                lifetime: 604800
    
    access_control:
        - { path: ^/login$, roles: PUBLIC_ACCESS }
        - { path: ^/register$, roles: PUBLIC_ACCESS }
        - { path: ^/recruiter-dashboard, roles: ROLE_RECRUITER }
        - { path: ^/evaluations, roles: ROLE_RECRUITER }
        - { path: ^/interviews, roles: ROLE_RECRUITER }
        - { path: ^/candidate, roles: ROLE_INTERVIEWEE }
        - { path: ^/admin, roles: ROLE_ADMIN }
=======
            custom_authenticator: App\Security\AppCustomAuthenticator
            logout:
                path: app_logout
                target: app_login
            remember_me:
                secret: '%kernel.secret%'
                lifetime: 604800
                path: /

    access_control:
        - { path: ^/login, roles: PUBLIC_ACCESS }
        - { path: ^/register, roles: PUBLIC_ACCESS }
        - { path: ^/connect/google, roles: PUBLIC_ACCESS }
        - { path: ^/qr/, roles: PUBLIC_ACCESS }
        - { path: ^/attachments/upload, roles: ROLE_USER }
        - { path: ^/admin, roles: ROLE_ADMIN }
        - { path: ^/users, roles: ROLE_ADMIN }
        - { path: ^/role, roles: ROLE_ADMIN }
        - { path: ^/joboffer/new, roles: ROLE_RECRUITER }
        - { path: ^/joboffer/\d+/edit, roles: ROLE_RECRUITER }
        - { path: ^/application/new, roles: ROLE_CANDIDATE }
        - { path: ^/application/\d+/accept, roles: ROLE_RECRUITER }
        - { path: ^/application/\d+/reject, roles: ROLE_RECRUITER }
        - { path: ^/application/\d+/review, roles: ROLE_RECRUITER }
        - { path: ^/, roles: ROLE_USER }

when@test:
    security:
        password_hashers:
            # By default, password hashers are resource intensive and take time. This is
            # important to generate secure password hashes. In tests however, secure hashes
            # are not important, waste resources and increase test times. The following
            # reduces the work factor to the lowest possible values.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
>>>>>>> OnboardingCoordination
